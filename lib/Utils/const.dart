import 'package:flutter/cupertino.dart';

const String Empty = '';

enum toastMessageVerdict { Error, WithoutError }

enum photoOptions { Camera, Gallery }

enum FileTypeOption { Image, Video }

enum profileSetup { Auth, Profile }
